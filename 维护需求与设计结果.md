

## 一、维护需求描述
软件维护是软件系统开发中很重要的一环，一般发生在软件投入使用后其运行出现故障、软件性能不够高，或者用户需求无法满足的情况下。维护的目的在于提高软件产品的质量，更好地满足软件使用者的需求。  
软件系统的维护有如下几种形式：纠正性维护、改善性维护、适应性维护、预防性维护。
- 纠正性维护，其目的是要纠正软件系统中存在的错误和缺陷；
- 改善性维护，其目的是要对软件系统进行改造以增加新的功能、修改已有的功能；
- 适应性维护，其目的是对软件系统进行改造，使其适应新的运行环境和平台，如操作系统、数据库管理系统等等；
- 预防性维护，其目的是对软件系统的整体架构等进行改造以提高其可靠性、可维护性、可扩展性等。

在小米便签开源软件的维护中，我们主要进行纠正性维护和改善性维护，其中改善性维护的任务是为小米便签开源软件完善功能。根据用户对于小米便签软件的使用，发现了软件中存在如下功能不完善的地方：

根据对“小米便签”开源软件功能的理解，目前版本的软件中的用户所创建的便签是对其他用户是可见的，没有相应的访问控制，不利于保护用户的个人隐私，因此可以考虑给便签访问提供密码保护的功能。该功能允许用户对便签设置访问密码，用户要访问便签时必须输入正确的密码才能看到相关的便签内容。具体的，该功能包括如下几个子功能：
1. 设置便签访问密码：为便签的访问设置相应的密码。
2. 解除便签访问密码：解除便签访问的密码保护。
3. 根据密码访问便签：在访问便签时要求输入密码，验证正确后才可以访问便签。

## 二、软件完善性设计
根据新增的软件需求和功能，对“小米便签”开源软件的相关程序包进行了完善性设计，受此影响的程序包包括：界面层的“ui”包，模型层的“model”包和数据层的“data”包。

### 1. 界面层“ui”包的调整
图1描述了经完善性维护后的“ui”程序包的设计类图，图中斜体文字表示受影响的设计类、方法和属性，主要对以下的设计元素进行了调整。
- 对“NoteEditActivity”类“initNoteScreen()”方法的内部实现算法进行了调整：当便签设置了密码时，先弹出密码校验对话框，校验通过后才展示正文。
- 对“NoteEditActivity”类“onPrepareOptionsMenu()”方法的内部实现算法进行了调整，在菜单中新增“设置访问密码”和“解除访问密码”入口。
- 对“NoteEditActivity”类“onOptionsItemSelected()”方法的内部实现算法进行了调整，响应设置/解除访问密码的操作并更新菜单状态。
- 在“NoteEditActivity”类中新增密码设置、密码解除与密码验证对话框逻辑，使用输入框完成密码校验。
- 在“NoteItemData”类中新增“mPassword”属性并对其构造方法“NoteItemData()”进行了调整，以保存便签的访问密码。
- 在“NoteItemData”类中新增“hasPassword()”方法，以判断便签是否具有访问密码。
- 在“NotesListItem”类中对“bind()”方法的内部实现算法进行了调整：对有密码且无提醒的便签显示锁图标。
![[Pasted image 20260512201002.png]]
						图1 经维护后的“ui”程序包的设计类图

### 2. 界面层“res”包的调整
对“res”包新增了密码相关菜单与字符串资源：
- 在“menu/note_edit.xml”和“menu/call_note_edit.xml”中新增“设置访问密码”“解除访问密码”菜单项。
- 在“values/strings.xml”及多语言资源中新增密码设置、校验与提示文案。
- 在“values/dimens.xml”中新增对话框内边距尺寸，统一输入对话框样式。
![[Pasted image 20260512201045.png]]
						图2 经维护后的“res”程序包的设计类图

### 3. 模型层“model”包的调整
图3描述了经完善性维护后的“model”程序包的设计类图，图中斜体文字表示受影响的设计类、方法和属性，主要就以下的设计元素进行了调整。
- 在“WorkingNote”类中新增“mPassword”属性并在构造时初始化，用于保存便签访问密码。
- 对“WorkingNote”类“loadNote()”方法进行了调整，从数据库读取密码字段。
- 在“WorkingNote”类中新增“setPassword()”“clearPassword()”“getPassword()”“hasPassword()”方法，支持保存、清除与判断访问密码。
![[Pasted image 20260512201110.png]]
					图3 经维护后的“model”程序包的设计类图

### 4. 数据层“data”包的调整
图4描述了经完善性维护后的“data”程序包的设计类图，图中斜体文字表示受影响的设计类、方法和属性，主要就以下的设计元素进行了调整。
- 对“Notes”类的“NoteColumns”接口进行了调整，新增“PASSWORD”字段以支持访问密码存取。
- 对“NotesDatabaseHelper”类的“CREATE_NOTE_TABLE_SQL”字符串实现进行了调整，以支持在数据库中记录便签访问密码。
- 数据库版本升级至 5，并新增升级逻辑，将“password”字段加入历史数据库。
![[Pasted image 20260512201137.png]]
								图4 经维护后的“data”包的设计类图

## 三、维护实现与修改结果
本节整理本次维护已在代码中完成的具体修改点，涵盖数据层、模型层与界面层。

### 1. 数据层与数据库变更
- 在“Notes.NoteColumns”中新增“PASSWORD”字段，作为便签访问密码的存储列。
- 将数据库版本升级至 5，并在建表 SQL 中新增“password”字段。
- 增加升级逻辑：从旧版本升级到 v5 时，自动为历史数据补充“password”字段。

### 2. 模型层数据读写
- “WorkingNote”新增“mPassword”属性，并在构造时初始化。
- “WorkingNote.loadNote()”从数据库读取密码字段。
- 新增“setPassword() / clearPassword() / getPassword() / hasPassword()”等方法，统一管理密码写入与清除。

### 3. 界面层交互与菜单
- “NoteEditActivity”增加密码校验流程：打开有密码的便签时先弹出验证对话框。
- 增加“设置访问密码 / 解除访问密码”菜单项，并根据是否已有密码动态显示。
- 新增设置密码、重复确认与解除密码对话框逻辑，并在成功设置后即时保存。

### 4. 列表展示与可见性提示
- “NoteItemData”加入密码字段读取，并提供“hasPassword()”。
- “NotesListItem.bind()”在无提醒且有密码的便签上显示锁图标，提示隐私保护。

### 5. 资源与文案补充
- 新增菜单项与对话框相关字符串，补齐交互提示。
- 新增对话框内边距尺寸，保证输入界面布局一致。
